version: 4.6.1.{build}

install:
  - cinst pester --version 3.4.3

build: false

test_script:
  - ps: .\psake-buildTester.ps1
  - ps: $res = Invoke-Pester -Path ".\tests" -OutputFormat NUnitXml -OutputFile TestsResults.xml -PassThru
  - ps: (New-Object 'System.Net.WebClient').UploadFile("https://ci.appveyor.com/api/testresults/nunit/$($env:APPVEYOR_JOB_ID)", (Resolve-Path .\TestsResults.xml))
  - ps: if ($res.FailedCount -gt 0) { throw "$($res.FailedCount) tests failed."}

after_test:
  - ps: Import-Module .\psake.psd1 -Force
  - ps: Invoke-psake .\build.psake.ps1

artifacts:
  - path: 'artifacts\*.nupkg'

deploy:
  - provider: NuGet
    server: https://www.myget.org/F/psake-yaml/api/v2/package
    api_key:
      secure: gVkXGqHTcaeG2LvitDXLjncrcpwp8OmpohC+bVBYCRaQ7pXWZFcnZLA2K3kwma2+
